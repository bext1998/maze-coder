#!/usr/bin/env bash
# Placeholder for the fixture repo root: lets the good-with-scripts fixture's prose
# mention of `scripts/sync-adapters.sh` resolve to a real repo-root script, so the
# validator's whitelist existence check passes on the positive case.
echo "fixture placeholder — not a real sync script"
