# Cursor Adapter

單一 `maze-coder-router.mdc` 只路由公開意圖；`.cursor/maze-coder/` 保存 28 個 canonical skills（25 個公開入口、3 個 internal）與自適應資源。

```bash
cp -r .cursor /your-project/
```

產物由 `scripts/sync-adapters.sh` 維護，請勿直接編輯。
